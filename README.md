# Training-free Truthfulness Detection via Sparse MLP Value Vectors
This repository is the official implementation of TruthV.

----

For log-likelihood, run `scripts/run_likelihood.sh`.

For NoVo, run `scripts/run_novo.sh`.

For TruthV, run `scripts/run_main.sh`.
